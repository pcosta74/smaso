# Properties file

SETUPS <- ordered(1:2, labels=c('4x4','12x5'))
INDICATORS <- ordered(1:3, labels=c('prices','wealth','utility'))
WEEKS <- 20

COL.BASE <- 4
COL.ALTER <- 2

PLOT.LWD <- 2

FILE.FORMATS <- factor(c('bmp','png','jpeg','tiff'))
FILE.WDT <- 640
FILE.HGT <- 640
FILE.RES <- 100